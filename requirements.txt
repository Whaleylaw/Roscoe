# Legal Agent Dependencies
# Install with: pip install -r requirements.txt

# Core DeepAgents Framework (from local libs/)
# Installed via langgraph.json dependencies field
# ./libs/deepagents

# LangChain Integrations
# NOTE: langchain 1.0.7 requires langgraph>=1.0.2,<1.1.0 and langchain-core>=1.0.4,<2.0.0
langchain>=1.0.2,<2.0.0
langchain-core>=1.0.4,<2.0.0
langchain-anthropic>=1.0.0,<2.0.0
langchain-openai>=0.3.0,<1.0.0

# Google Workspace Integrations (Gmail, Calendar)
# Updated to 3.x for compatibility with langchain 1.0.x
langchain-google-community>=3.0.0,<4.0.0
google-auth-oauthlib>=1.0.0  # Required for Gmail and Calendar OAuth authentication

# MCP Integration for Supabase and Tavily
langchain-mcp-adapters>=0.1.0

# LangGraph for Agent Runtime
# Updated to 1.x to match langchain 1.0.7 requirement (langgraph>=1.0.2,<1.1.0)
langgraph>=1.0.2,<1.1.0
langgraph-checkpoint-postgres>=2.0.0,<4.0.0

# RunLoop Sandboxed Execution
runloop-api-client>=0.66.1

# Database and Storage
supabase>=2.0.0
psycopg[binary,pool]>=3.0.0

# Utilities
python-dotenv>=1.0.0

# Text-to-Speech (ElevenLabs)
elevenlabs>=1.0.0
langchain-community>=0.3.0  # Required for ElevenLabsText2SpeechTool

# Telegram Bot Integration
python-telegram-bot>=21.0.0  # For Telegram bot interface with async support (polling mode)
fastapi>=0.115.0  # FastAPI web framework for webhook mode
uvicorn[standard]>=0.32.0  # ASGI server for FastAPI

# Optional: Observability
# langsmith>=0.1.0  # Uncomment for LangSmith tracing
